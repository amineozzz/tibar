/** @type {import('next').NextConfig} */
const nextConfig = {

    // add dashboard.konnect.network to the list of domains
    images: {
        domains: ['dashboard.konnect.network'],
    },
};


export default nextConfig;
